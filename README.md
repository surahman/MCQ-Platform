# Multiple Choice Question Platform




### Apache Cassandra

Create containers:
```bash
docker compose up -d
```

Destroy containers:
```bash
docker compose down
```

**Cassandra:**
- Username : `admin`
- Password : `root`
- DB name  : `cassandra`
